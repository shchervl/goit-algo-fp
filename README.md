# goit-algo-fp

Фінальний проєкт курсу з алгоритмів та структур даних (GoIT × Neoversity).

## Налаштування

Проєкт використовує [uv](https://docs.astral.sh/uv/) для керування залежностями.

```bash
uv sync
```

Це створить `.venv/`, встановить залежності та зареєструє виконуваний шорткат `pythagoras-tree`.

---

## Завдання 1. Структури даних. Сортування. Робота з однозв'язним списком

Реалізовано однозв'язний список з трьома операціями:

1. **Реверсування** — `LinkedList.reverse()`: змінює посилання між вузлами на місці, без створення нових вузлів.
2. **Сортування** — `LinkedList.sort()`: сортування злиттям (merge sort) за O(n log n).
3. **Об'єднання двох відсортованих списків** — `merge_sorted(a, b)`: повертає новий відсортований список без модифікації вхідних.

Файли: `task_01/linked_list.py`, тести в `task_01/tests/test_linked_list.py`.

### Запуск

```bash
# Запуск тестів (77 тестів)
uv run pytest task_01/

# Інтерактивне використання
uv run python -c "
from task_01.linked_list import LinkedList, merge_sorted

ll = LinkedList([3, 1, 4, 1, 5, 9, 2, 6])
ll.sort()
print(ll.to_list())  # [1, 1, 2, 3, 4, 5, 6, 9]

ll.reverse()
print(ll.to_list())  # [9, 6, 5, 4, 3, 2, 1, 1]

a = LinkedList([1, 3, 5])
b = LinkedList([2, 4, 6])
print(merge_sorted(a, b).to_list())  # [1, 2, 3, 4, 5, 6]
"
```

---

## Завдання 2. Рекурсія. Фрактал «дерево Піфагора»

Дерево Піфагора через рекурсію з інтерактивним UI на tkinter. Підтримує два стилі візуалізації:

- **гілки** — branch-style фрактал (відрізки), симетричний при будь-якому куті
- **квадрати** — класична Піфагорова композиція з квадратами та прямокутними трикутниками (симетрична лише при α=45°)

Структура пакета `task_02/`:

| Модуль | Призначення |
|---|---|
| `geometry.py` | Чисті рекурсивні генератори (`generate_branches`, `generate_squares`, `square_corners`) |
| `styling.py` | Колір і товщина за глибиною |
| `pythagoras_tree.py` | Tkinter UI: радіо-перемикач стилю, повзунки глибини/кута |
| `__main__.py` | Точка входу: запускає `run_ui()` |

### Запуск

```bash
uv run pythagoras-tree
```

Керування у вікні:
- радіо-перемикач **«Стиль»** — *гілки* / *квадрати*
- повзунок **«Рівень рекурсії»** (0–12)
- повзунок **«Кут розгалуження»** (0°–90°)

---

## Завдання 3. Дерева, алгоритм Дейкстри

Алгоритм Дейкстри для пошуку найкоротших шляхів у зваженому графі з використанням бінарної купи (`heapq`). Складність — O((V + E) log V).

Структура пакета `task_03/`:

| Модуль | Призначення |
|---|---|
| `graph.py` | `Graph[T]` — generic зважений граф (orієнтований/неорієнтований) на списку суміжності |
| `dijkstra.py` | `dijkstra(graph, start)` — мін-купа з lazy deletion; повертає `DijkstraResult[T]` (NamedTuple з `distances`, `predecessors`, `path_to()`) |
| `demo.py` | Класичний приклад: 6 вершин, 9 ребер, друк відстаней та шляхів від `A` |
| `tests/` | 62 тести: граф, Дейкстра, реконструкція шляху, цикли, `None`, граничні випадки |

Реалізація використовує **lazy deletion** замість `decrease-key`: при знаходженні кращої ваги нова пара `(dist, node)` штовхається у купу; старі (stale) пари пропускаються при витяганні через перевірку `dist > distances[u]`.

### Запуск

```bash
uv run python -m task_03.demo
```

Очікуваний вивід (класичний приклад з Вікіпедії):

```
Куди   Відстань   Шлях
----------------------------------------
A      0.0        A
B      7.0        A → B
C      9.0        A → C
D      20.0       A → C → D
E      20.0       A → C → F → E
F      11.0       A → C → F
```

### Використання в коді

```python
from task_03.graph import Graph
from task_03.dijkstra import dijkstra

g: Graph[str] = Graph(directed=False)
g.add_edge("A", "B", 7)
g.add_edge("A", "C", 9)
g.add_edge("B", "C", 10)

result = dijkstra(g, start="A")
print(result.distances["C"])    # 9.0
print(result.path_to("C"))      # ['A', 'C']

# Tuple-unpacking теж працює (для зворотньої сумісності):
distances, predecessors = dijkstra(g, "A")
```

---

## Завдання 4. Візуалізація бінарної купи

Функція, що візуалізує бінарну купу як дерево. Реалізація використовує базовий код з умови (Node + networkx + matplotlib), розширений `build_tree_from_heap(heap)` — конструює дерево з масиву купи за класичною формулою індексів (`i` → діти `2i+1`, `2i+2`).

Структура пакета `task_04/`:

| Модуль | Призначення |
|---|---|
| `heap_tree.py` | `Node`, `build_tree_from_heap(heap, color_fn, validate)` — масив → дерево; `_build_graph` + `_layout` — окремо нетворкс-граф і розкладка; `draw_heap(heap, save, color_fn, validate)` — візуалізація |
| `demo.py` | Будує min-купу через `heapq.heapify`, малює дерево |
| `tests/` | 32 тести: Node, побудова з купи, валідація min-heap, розкладка, побудова графа, дублікати, smoke-тести draw_heap |

### Запуск

```bash
uv run python -m task_04.demo
```

Інтерактивно у коді:
```python
import heapq
from task_04.heap_tree import draw_heap

data = [10, 4, 5, 1, 3, 7, 2, 8, 6, 9]
heapq.heapify(data)
draw_heap(data, save="heap.png")  # або без save= для інтерактивного вікна
```

---

## Завдання 5. Візуалізація обходу бінарного дерева

Ітеративні обходи **DFS (через стек)** та **BFS (через чергу)** з кольоровою візуалізацією: кожен вузол отримує унікальний hex-колір, від темного (відвіданий першим) до світлого (відвіданий останнім). Без рекурсії.

Структура пакета `task_05/`:

| Модуль | Призначення |
|---|---|
| `traversal.py` | `dfs_iterative(root)` — стек; `bfs_iterative(root)` — `collections.deque`; `shade_gradient(n)` — `n` hex-кольорів від темного до світлого; `apply_traversal_colors(seq)` — фарбує вузли in-place; `draw_traversal(root, order, save)` — обхід + візуалізація |
| `demo.py` | Симетричне дерево з 7 вузлів: DFS і BFS поруч |
| `tests/` | 30 тестів: DFS/BFS порядок, градієнт кольорів, унікальність, інваріант темний→світлий |

Базується на `task_04` — імпортує `Node` та `draw_tree`. У `task_04/heap_tree.py` винесено `draw_tree(root, title, save)` як публічну функцію для малювання довільного бінарного дерева.

### Запуск

```bash
uv run python -m task_05.demo
```

Інтерактивно у коді:
```python
from task_04.heap_tree import Node
from task_05.traversal import draw_traversal

root = Node(1)
root.left = Node(2); root.right = Node(3)
root.left.left = Node(4); root.left.right = Node(5)

seq = draw_traversal(root, order="dfs", save="dfs.png")
print([n.val for n in seq])  # [1, 2, 4, 5, 3]
```

---

## Завдання 6. Жадібні алгоритми та динамічне програмування

Класична задача 0/1-ранця: вибір страв з обмеженим бюджетом для максимізації калорій. Реалізовано **два підходи** для порівняння:

- `greedy_algorithm(items, budget)` — жадібний: сортує за калорії/вартість, бере доки бюджет дозволяє. O(n log n). Швидкий, але **не гарантує оптимуму** для 0/1-ранця.
- `dynamic_programming(items, budget)` — DP-таблиця `dp[i][b]` з backtracking. O(n·B). **Завжди оптимальний** результат.

Обидві функції повертають `Selection(items, total_cost, total_calories)` — NamedTuple з підтримкою tuple-unpacking і attribute-access.

Структура пакета `task_06/`:

| Модуль | Призначення |
|---|---|
| `knapsack.py` | `greedy_algorithm`, `dynamic_programming`, `Selection` |
| `demo.py` | Порівняння на бюджетах 50/80/100/150 |
| `tests/` | 19 тестів: коректність, інваріант DP ≥ greedy, граничні випадки |

### Запуск

```bash
uv run python -m task_06.demo
```

При бюджеті **100** DP знаходить `pizza+cola+potato+pepsi = 970` кал (вписався точно), а greedy зупиняється на `cola+potato+pepsi+hot-dog = 870` кал (лишає 20 неспожитих). Це канонічний приклад **суб-оптимальності жадібного алгоритму** для 0/1-ранця.

---

## Тести

```bash
uv run pytest                # усі тести
uv run pytest task_01/       # тільки задача 1 (77 тестів)
uv run pytest task_03/       # тільки задача 3 (62 тести)
uv run pytest task_04/       # тільки задача 4 (34 тести)
uv run pytest task_05/       # тільки задача 5 (30 тестів)
uv run pytest task_06/       # тільки задача 6 (19 тестів)
```

**task_01** (77 тестів): реверс, сортування злиттям, об'єднання, граничні випадки (порожні списки, `None`, різні типи даних: float, bool, кортежі, кастомний клас з `__lt__`, NaN), а також інваріант «реверс саме переставляє посилання, не створюючи нові вузли».

**task_03** (62 тести): граф (неорієнтований/орієнтований, кратні ребра, петлі, валідація від'ємних ваг), Дейкстра (класичний приклад, недосяжні вершини, lazy-deletion, цикли в обох режимах, `None` як вершина, тіе́д шляхи, числові/кортежні ідентифікатори, ланцюг 100 вершин), реконструкція шляху (включно з порожнім словником предків).
